# Pin loosely so installs work on current Python (3.10+). For an exact reproduction
# of the 2020 paper environment, use a dedicated conda env with older torch.
numpy>=1.20
matplotlib>=3.5
rasterio>=1.3
scikit-learn>=1.0
tensorboard>=2.10
torch>=1.13
torchvision>=0.14
tqdm>=4.60
shapely>=2.0
